# interop

The Containerfiles the image preparation command builds from the pinned base
images, one directory per built image. The gate never builds an image: it
verifies the recorded identifier of the one preparation built.

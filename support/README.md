# support

Every pinned value, in one directory. Each TOML document here is the one place
its values are written down, each value carries its reason, and the source
reads them through the typed loader in `src/harness/values.ts` rather than
restating any of them.

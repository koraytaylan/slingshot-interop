# src/run

The orchestration of one run and the report it writes: resolving both sides,
verifying the prepared inputs, starting what the run needs, discovering the
scenarios in their deterministic order, and one report naming every byte the
run was about.

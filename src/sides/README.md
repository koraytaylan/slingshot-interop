# src/sides

The pinning of each sibling and the per-side runtimes built on it: the
resolution of released or candidate bytes from their pinning documents, the
author runtime that installs the resolved bundle, and the client runner that
mounts the supplied executable. The harness never builds, fetches, or caches a
sibling; this layer resolves bytes somebody else recorded or refuses.
